-- Test script with meme functions
local function getBasedRating(cringe)
  if cringe > 9000 then
    return "No cap fr fr"
  else
    return "Mid + L + ratio"
  end
end

print(getBasedRating(10000))
print("This script was obfuscated with GGH - the strongest protector")
